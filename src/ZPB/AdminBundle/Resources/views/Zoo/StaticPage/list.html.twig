{% extends "ZPBAdminBundle:Layouts:master.html.twig" %}

{% block title %}Liste des pages statiques | ZooParc de Beauval{% endblock %}
{% set active = "list_static_pages" %}

{% block content %}
    <div class="row">
        <div class="column-12">
            <div class="tile">
                <div class="tile-header">Liste des pages statiques, ZooParc de Beauval</div>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="column-12">
            <div class="tile">
                <table class="table table-striped table-hover">
                    <thead>
                    <tr>
                        <th>#Id</th>
                        <th>Nom</th>
                        <th>Site</th>

                        <th>Modifier</th>
                        <th>Supprimer</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% if entities|length < 1 %}
                        <tr class="warning"><td colspan="5">Il n'y a pas de pages statiques enregistrées.</td></tr>
                    {% else %}
                        {% for entity in entities %}
                            <tr>
                                <td>{{ entity.id }}</td>
                                <td>{{ entity.name }}</td>
                                <td>{{ entity.owner.name }}</td>


                                <td><a class="btn btn-neutral" href="{{ path("zpb_admin_zoo_static_pages_update", {id: entity.id}) }}"><i class="fa fa-edit"></i></a></td>
                                <td><a class="btn btn-neutral alert-delete" href="#"{#href="{{ path("zpb_admin_animations_delete", {id: specie.id, _token: csrf_token('delete_animation')}) }}"#} data-name="{{ entity.name }}"><i class="fa fa-trash-o"></i></a></td>
                            </tr>
                        {% endfor %}
                    {% endif %}
                    </tbody>
                </table>

            </div>
        </div>
    </div>
{% endblock %}
